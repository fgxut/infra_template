{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "s3:*"
            ],
            "Resource": [
                "arn:aws:s3:::${ENV_ABBR}-${NAME}-ecs-access-logs-${ACCOUNT_ID}/*",
                "arn:aws:s3:::${ENV_ABBR}-${NAME}-ecs-access-logs-${ACCOUNT_ID}"
            ]
        }
    ]
}
